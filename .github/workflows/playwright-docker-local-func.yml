name: Local Playwright Docker Functional

on: [workflow_dispatch]

jobs:
  test-functional:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        browser: [chromium, firefox, webkit]
    steps:
      - uses: actions/checkout@v4
      - run: mkdir -p test_results
      - run: pytest tests/tests_ui/test_login_page.py --browser ${{ matrix.browser }}
      - name: Save Playwright Traces (locally)
        if: ${{ failure() }}
        run: |
          cp -r test_results/* reports/playwright-traces-${{ matrix.browser }}-tests_functional || true
